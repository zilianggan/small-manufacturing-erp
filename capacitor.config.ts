import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.sengjie.app',
  appName: 'Seng Jie Engineering ERP System',
  webDir: 'dist'
};

export default config;
